{{- range .Values.secrets }}
{{ if eq .type "Opaque" }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
type: {{ .type }}
data:
  {{- range .data }}
  {{ .key }}: {{ .value -}}
  {{ end }}
{{ else }}
  # TODO: Add other secret types
{{ end }}
---
{{- end }}
